services:
  giffgaff-reminder:
    build: .
    container_name: giffgaff-reminder
    restart: unless-stopped
    ports:
      - "18080:8000"
    env_file:
      - ${ENV_FILE:-.env}
    volumes:
      - ./data:/app/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
